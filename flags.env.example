# Cybersecurity Lab Flag Configuration
# Copy this file to 'flags.env' and customize the values below
# The flags.env file will be automatically loaded by docker-compose

# Flag content that will be placed in the password-protected zip file
# This is what students will find after cracking the zip
FLAG_CONTENT=f4k3_fl4g_f0r_t3st1ng

# Password for the zip file containing the flag
# Students need to crack this password to access the flag
ZIP_PASSWORD=maggie

# Location where the flag zip file will be placed inside the container
# Default: /var/log/flag.zip (mimics real-world log file placement)
FLAG_LOCATION=/var/log/asdfgnarlyzxcv.zip